/* Developer Tools-only breadcrumb hierarchy. */
(function () {
  "use strict";
  // DevTools breadcrumb generation retained; Home items now use the same
  // algolassi-toolmenu-category class as category breadcrumbs.
})();
